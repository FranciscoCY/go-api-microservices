apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  config.yaml: |
    database:
      host: db
      port: 5432
      user: postgres
      password: postgres
      name: mydb
      sslmode: disable